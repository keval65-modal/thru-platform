# Environment Variables Template
# Copy this file to .env.local for LOCAL DEVELOPMENT ONLY
# For Vercel deployment, add these variables in Vercel Dashboard → Settings → Environment Variables

# Firebase Configuration
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id_here
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id_here
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id_here

# Google Maps API Key for grocery shopping features
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Vendor API URL for grocery services (V4)
NEXT_PUBLIC_VENDOR_API_URL=https://merchant.kiptech.in/api

# reCAPTCHA Configuration (Temporarily Disabled)
# NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your_recaptcha_v2_site_key_here
# RECAPTCHA_SECRET_KEY=your_recaptcha_v2_secret_key_here

# Note: reCAPTCHA is temporarily disabled to allow the app to work
# Phone OTP will function without bot protection until reCAPTCHA is re-enabled

# ========================================
# DEPLOYMENT INSTRUCTIONS:
# ========================================
# 1. For LOCAL DEVELOPMENT:
#    - Copy this file to .env.local
#    - Fill in your actual values
#    - .env.local is already in .gitignore
#
# 2. For VERCEL DEPLOYMENT:
#    - Go to Vercel Dashboard → Your Project → Settings → Environment Variables
#    - Add each variable with its production value
#    - Redeploy your application
#
# 3. Required for Grocery Shopping System:
#    - NEXT_PUBLIC_GOOGLE_MAPS_API_KEY (get from Google Cloud Console)
#    - NEXT_PUBLIC_VENDOR_API_URL (already provided above)


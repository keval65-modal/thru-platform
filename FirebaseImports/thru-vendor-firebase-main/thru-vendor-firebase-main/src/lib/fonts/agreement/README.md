# Agreement PDF fonts (serverless bundle)

Noto Sans Devanagari (OFL-1.1) for Hindi/Marathi signed agreement PDFs.

Stored under `src/lib/fonts` (not `src/assets`, which is gitignored) so Vercel includes them in the agreement sign API trace.
